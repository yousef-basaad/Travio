// Invoices, payments, refunds - tenant-scoped financial records.
// Follow the pattern established in features/customers and features/bookings:
// components/ (UI), api/ (queries/mutations), schemas/ (Zod), types/ (local types).
export function FinancePlaceholder() {
  return <div className="text-muted-foreground">Finance - implement following features/bookings pattern.</div>;
}
