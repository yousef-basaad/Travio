// Visa application tracking per booking (status, passport, submission).
// Follow the pattern established in features/customers and features/bookings:
// components/ (UI), api/ (queries/mutations), schemas/ (Zod), types/ (local types).
export function VisaPlaceholder() {
  return <div className="text-muted-foreground">Visa - implement following features/bookings pattern.</div>;
}
