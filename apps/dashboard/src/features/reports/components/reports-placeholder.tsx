// Cross-feature analytics (revenue, booking volume, agent performance).
// Follow the pattern established in features/customers and features/bookings:
// components/ (UI), api/ (queries/mutations), schemas/ (Zod), types/ (local types).
export function ReportsPlaceholder() {
  return <div className="text-muted-foreground">Reports - implement following features/bookings pattern.</div>;
}
